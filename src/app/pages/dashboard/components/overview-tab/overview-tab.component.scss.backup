@use 'src/styles/theme' as *;

.overview-container {
  display: flex;
  flex-direction: column;
  gap: $spacing-xl;
  animation: fadeIn 0.3s ease;

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
}

.loading-state {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: $spacing-lg;

  .skeleton-card {
    height: 200px;
    background: linear-gradient(90deg, rgba($color-text-primary, 0.05) 25%, rgba($color-text-primary, 0.1) 50%, rgba($color-text-primary, 0.05) 75%);
    background-size: 200% 100%;
    border-radius: $border-radius-lg;
    animation: shimmer 2s infinite;
  }

  @keyframes shimmer {
    0% {
      background-position: 200% 0;
    }
    100% {
      background-position: -200% 0;
    }
  }
}

// ====================================
// CARDS
// ====================================

.card {
  background-color: $color-bg-surface;
  border: 1px solid rgba($color-text-primary, 0.08);
  border-radius: $border-radius-lg;
  padding: $spacing-lg;
  transition: $transition-base;
  cursor: pointer;

  &:hover {
    border-color: rgba($color-primary-accent, 0.2);
    background-color: $color-bg-elevated;
    transform: translateY(-4px);
    box-shadow: $shadow-md;
  }

  &.card-elevated {
    background: linear-gradient(135deg, rgba($color-bg-elevated, 0.8), rgba($color-bg-surface, 0.8));
    border: 1px solid rgba($color-primary-accent, 0.1);
    box-shadow: $shadow-md;
  }
}

.balance-card {
  grid-column: 1 / -1;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  min-height: 200px;
  position: relative;
  overflow: hidden;

  &::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba($color-primary-accent, 0.1), transparent);
    border-radius: 50%;
    filter: blur(80px);
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: relative;
    z-index: 1;

    .card-label {
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: $color-text-secondary;
      font-weight: 500;
    }

    .card-icon {
      font-size: 24px;
    }
  }

  .balance-amount {
    font-size: 42px;
    font-weight: 700;
    background: linear-gradient(135deg, $color-primary-accent, $color-secondary-accent);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    position: relative;
    z-index: 1;
    margin: $spacing-lg 0;
  }

  .balance-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: relative;
    z-index: 1;
    font-size: 12px;
    padding-top: $spacing-md;
    border-top: 1px solid rgba($color-text-primary, 0.05);
  }
}

.cards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: $spacing-lg;
}

.card {
  &.card-income,
  &.card-expense,
  &.card-savings {
    display: flex;
    align-items: center;
    gap: $spacing-lg;

    .card-icon {
      font-size: 32px;
      flex-shrink: 0;
    }

    .card-content {
      display: flex;
      flex-direction: column;
      gap: $spacing-sm;

      .card-label {
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        color: $color-text-secondary;
        font-weight: 500;
      }

      .card-value {
        font-size: 20px;
        font-weight: 700;
      }
    }
  }

  &.card-income {
    border-left: 3px solid $color-success;

    &:hover {
      border-left-color: lighten($color-success, 10%);
    }
  }

  &.card-expense {
    border-left: 3px solid $color-danger;

    &:hover {
      border-left-color: lighten($color-danger, 10%);
    }
  }

  &.card-savings {
    border-left: 3px solid $color-secondary-accent;

    &:hover {
      border-left-color: lighten($color-secondary-accent, 10%);
    }
  }
}

// ====================================
// EXPENSE SPLIT
// ====================================

.expense-split-container,
.recent-activity-container {
  display: flex;
  flex-direction: column;
  gap: $spacing-lg;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;

  h3 {
    font-size: 18px;
    font-weight: 600;
  }
}

.expense-split-chart {
  position: relative;
  width: 100%;
  max-width: 300px;
  margin: 0 auto;
  aspect-ratio: 1;

  .chart-center {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    z-index: 2;

    .chart-value {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: $spacing-sm;

      .total-expenses {
        font-size: 24px;
        font-weight: 700;
        color: $color-text-primary;
      }

      .label {
        font-size: 12px;
        color: $color-text-secondary;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }
    }
  }

  .donut-chart {
    width: 100%;
    height: 100%;
    transform: rotate(-90deg);

    .donut-base {
      fill: none;
      stroke: rgba($color-text-primary, 0.08);
      stroke-width: 30;
    }
  }
}

.expense-list {
  display: flex;
  flex-direction: column;
  gap: $spacing-md;
}

.expense-item {
  display: flex;
  flex-direction: column;
  gap: $spacing-sm;
  padding: $spacing-md;
  background-color: rgba($color-bg-elevated, 0.4);
  border-radius: $border-radius-md;
  transition: $transition-fast;

  &:hover {
    background-color: rgba($color-bg-elevated, 0.6);
  }

  .expense-info {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 13px;

    .expense-name {
      font-weight: 500;
      color: $color-text-primary;
    }

    .expense-percentage {
      color: $color-primary-accent;
      font-weight: 600;
    }
  }

  .expense-bar {
    height: 6px;
    background-color: rgba($color-text-primary, 0.08);
    border-radius: 3px;
    overflow: hidden;

    .expense-bar-fill {
      height: 100%;
      background: linear-gradient(90deg, $color-primary-accent, $color-secondary-accent);
      border-radius: 3px;
      transition: width 0.5s ease;
    }
  }

  .expense-amount {
    font-size: 13px;
    font-weight: 600;
    color: $color-text-secondary;
  }
}

// ====================================
// RECENT ACTIVITY
// ====================================

.activity-list {
  display: flex;
  flex-direction: column;
  gap: $spacing-md;
}

.activity-item {
  display: grid;
  grid-template-columns: auto 1fr auto auto;
  align-items: center;
  gap: $spacing-lg;
  padding: $spacing-md;
  background-color: rgba($color-bg-elevated, 0.4);
  border-radius: $border-radius-md;
  border: 1px solid rgba($color-text-primary, 0.05);
  transition: $transition-base;

  &:hover {
    background-color: rgba($color-bg-elevated, 0.6);
    border-color: rgba($color-primary-accent, 0.2);
  }

  .activity-icon {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: rgba($color-bg-app, 0.5);
    border-radius: 50%;
    font-size: 18px;

    &.icon-income {
      background-color: rgba($color-success, 0.1);
    }

    &.icon-expense {
      background-color: rgba($color-danger, 0.1);
    }
  }

  .activity-details {
    display: flex;
    flex-direction: column;
    gap: $spacing-xs;

    .activity-description {
      font-size: 13px;
      font-weight: 500;
      color: $color-text-primary;
    }

    .activity-category {
      font-size: 12px;
      color: $color-text-secondary;
    }
  }

  .activity-amount {
    font-size: 14px;
    font-weight: 700;

    &.amount-income {
      color: $color-success;
    }

    &.amount-expense {
      color: $color-danger;
    }
  }

  .activity-status {
    font-size: 11px;
    font-weight: 600;
    text-transform: capitalize;
    padding: $spacing-xs $spacing-sm;
    border-radius: $border-radius-sm;
    background-color: rgba($color-text-secondary, 0.1);
    color: $color-text-secondary;

    &.status-success {
      background-color: rgba($color-success, 0.1);
      color: $color-success;
    }

    &.status-pending {
      background-color: rgba($color-warning, 0.1);
      color: $color-warning;
    }

    &.status-failed {
      background-color: rgba($color-danger, 0.1);
      color: $color-danger;
    }
  }

  @media (max-width: 768px) {
    grid-template-columns: auto 1fr;
    gap: $spacing-md;

    .activity-amount,
    .activity-status {
      grid-column: 2;
      justify-self: start;
      font-size: 12px;
    }
  }
}

// ====================================
// CHAT SECTION
// ====================================

.chat-section {
  margin-top: $spacing-xl;
  
  ::ng-deep .chat-container {
    height: 700px; // Make it taller
    max-height: 700px;
  }
}
